## Task: Second Opinion Review — Solidity Audit Tool Accuracy

We are building `sol-checker`, an npm CLI that uses LLMs to audit Solidity smart contracts.
We run it against well-known production contracts and manually verify the output for accuracy.

Today's target: **{{CONTRACT_NAME}}**

### Files to review

Please read these files in order:

1. `{{CONTRACT_PATH}}`
   — The Solidity source code that was audited

2. `{{REPORT_PATH}}`
   — The audit report generated by sol-checker.
     Some findings may have been auto-downgraded from higher severity and marked ⚠ UNVERIFIED.

3. `~/projects/sol-checker/prompts/checker-system.md`
   — The system prompt that guides the LLM auditor

4. `~/projects/sol-checker/prompts/verifier-system.md`
   — The system prompt that guides the LLM verifier (second-pass fact-checker)

### What I need from you

**A. Report accuracy (file 2 vs file 1):**
For each finding in the report, evaluate against the actual Solidity source:
- Is the finding factually correct? (line numbers, code behavior, stated impact)
- Is it a true positive, false positive, or hallucination?
- Is the severity appropriate given the contract's role and the value it manages?

**B. Detection gaps (file 1 vs file 2):**
Are there security-relevant issues in the contract that the report missed entirely?
Focus on: governance risks, trust boundary assumptions, privilege escalation paths, and economic attack vectors.

**C. Prompt quality (files 3 and 4):**
- Do the system prompts create any systematic bias (toward over-reporting or under-reporting)?
- Is the guidance on governance risk assessment balanced — does it avoid both "flag everything as centralization risk" and "dismiss everything as design intent"?
- Any contradictions between the two prompts?

Output a structured assessment with clear verdicts per finding.
Do not assume any prior conclusions — form your own opinion from the source code.
